# Profile cleanup V101

## Что изменено

- Убран верхний декоративный блок страницы профиля с заголовком «Профиль» и подписью «Личное пространство без визуальной каши».
- Со страницы `/profile` удалены все публичные упоминания DFSN: статусный чип, кнопка перехода и строка в блоке «О себе».
- Пассивная ошибка загрузки постов профиля больше не показывается пользователю как красный alert.

## Проверка

- `node scripts/public-beta-qa.mjs` — ready.
- `node scripts/verify-launch.mjs` — ready.
- `node scripts/alpha-qa.mjs` в текущем контейнере не завершился из-за зависания системного Python-процесса окружения, не из-за кода проекта.
